@model IEnumerable<EducacionalAPIConexaoDB.Models.Turma>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AnoTurma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SerieTurma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LetraTurma)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AnoTurma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SerieTurma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LetraTurma)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TurmaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TurmaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TurmaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
