@model IEnumerable<EducacionalAPIConexaoDB.Models.Classroom>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.YearGrade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GradeClassRoom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LetterGrade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.YearGrade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GradeClassRoom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LetterGrade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClassroomId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ClassroomId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ClassroomId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
